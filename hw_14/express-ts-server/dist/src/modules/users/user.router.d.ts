import { Router } from "express";
import { UserController } from "./user.controller";
import { UserRepository } from "./user.entity";
export declare function createUserRouter(controller: UserController, repo: UserRepository): Router;
//# sourceMappingURL=user.router.d.ts.map